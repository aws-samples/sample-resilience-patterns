# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: "drs-ec2: e2e"
on:
  pull_request:
    paths:
      - drs-ec2/**
  workflow_dispatch: {}
jobs:
  e2e:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: drs-ec2
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "20"
      - uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::563688183446:role/github-actions-drs-ec2
          aws-region: us-east-2
          role-duration-seconds: 14400
      - run: npm ci
      - run: npx projen test
      - name: Pre-flight cleanup (idempotent)
        run: chmod +x cleanup.sh && ./cleanup.sh || true
      - name: Deploy
        run: make deploy
      - name: Resting state
        run: make status
      - name: Fail-over / fail-back cycle
        run: make rehearse-cycle LEGS=2
      - name: Refresh AWS credentials (pre-cleanup)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::563688183446:role/github-actions-drs-ec2
          aws-region: us-east-2
      - name: Cleanup on success
        if: success()
        run: ./cleanup.sh
    timeout-minutes: 300
