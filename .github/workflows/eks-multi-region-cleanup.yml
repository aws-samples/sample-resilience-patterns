# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: "eks-multi-region: cleanup"
on:
  workflow_dispatch:
    inputs:
      assets_bucket_prefix:
        description: ASSETS_BUCKET_PREFIX of the run to clean (e2e uses eks-multi-region-<sha6>)
        required: false
        default: eks-multi-region
        type: string
jobs:
  cleanup:
    runs-on: ubuntu-24.04-arm
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: eks-multi-region
    steps:
      - uses: actions/checkout@v6
      - uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::563688183446:role/github-actions-eks-multi-region
          aws-region: us-east-2
          role-duration-seconds: 28800
      - run: chmod +x cleanup.sh && ./cleanup.sh
    timeout-minutes: 60
env:
  AWS_REGION: us-east-2
  AWS_DEFAULT_REGION: us-east-2
  ROLE_ARN: arn:aws:iam::563688183446:role/eks-multi-region-cfn-exec
  ASSETS_BUCKET_PREFIX: ${{ inputs.assets_bucket_prefix || 'eks-multi-region' }}
